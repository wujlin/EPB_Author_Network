# Data Directory

Place the cleaned EPB input CSV here, or pass its path with `--input-csv`.

Default expected filename:

```text
output_final_with_source_revised_cleaned_authorfix.csv
```

The CSV should include at least:

- `title`
- `year`
- `authors`
- `authors_full_final`
- `doi`
